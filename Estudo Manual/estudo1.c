#include <stdio.h>

#define MAX_MARK 20.0
#define MIN_MARK 9.5

main () 
{
    /*
    int c;
    c = getchar();
    while (c != EOF)
    {
        putchar(c);
        c = getchar();
    }
    */

    /*de maneira a conservar espaço vertical*/

    int c;
    while ((c=getchar())!=EOF)
    {
        putchar(c);
    }
    
}